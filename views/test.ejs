<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Modal Include</title>
</head>
<body>
    <h1>Test Page for Modal Include</h1>
    <p>If you see this page, it means the route is working.</p>

    <!-- Attempt to include the modal directly -->
    <% try { %>
        <%- include('partials/tempModal.ejs', { post: post }) %>
    <% } catch (error) { %>
        <p>Error including partial: <%= error.message %></p>
    <% } %>

    <p>End of test page.</p>
</body>
</html>
